import __LOWER_REPLACE__, { State as __PASCAL_REPLACE__State } from "./__LOWER_REPLACE__";

export type RootState = {
    __LOWER_REPLACE__: __PASCAL_REPLACE__State;
};

export const reducers = { __LOWER_REPLACE__ };
